{
  "mcpServers": {
    "haloscan": {
      "command": "npx",
      "args": [
        "-y",
        "@occirank/haloscan-server@2.0.21",
        "start"
      ],
      "env": {
        "HALOSCAN_API_KEY": "${HALOSCAN_API_KEY}"
      }
    }
  }
}
